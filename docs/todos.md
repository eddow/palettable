# TODOs

## Generic types

- Color
- Range
- vector (at least 2D)
- breadcrumbs

## Ideas

- In edition mode, have a contextual menu for tools and toolbars (tracks?)
- resize longitudinal tools + drawer-tools (sliders, ...)
- catch the wheel when slider:hover to change the value
- derived values: any type of value can become an `enum` if given enough predefined values - cf. `enum-from` virtuals
- keywords (both for tools and options) usage:
  - command-box filtering
  - sub-set specification per filter